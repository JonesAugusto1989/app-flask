name: Testar_Noturno

on:
  schedule:
    - cron: '0 23 * * * '
  push:
    branches:
      - main
jobs:
  teste_norturno:
    runs-on: self-hosted
    steps:
      - name: "Iniciando JOB"
        run: | 
          echo "inicando JOB em %(date)"